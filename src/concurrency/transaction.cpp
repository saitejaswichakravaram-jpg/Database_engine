// transaction.cpp – all implementation is inline in the header for now.
// This file exists so CMake compiles a translation unit for the concurrency
// module (necessary for the static library linkage).
#include "concurrency/transaction.h"
